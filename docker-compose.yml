services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: thelot
      POSTGRES_PASSWORD: thelot
      POSTGRES_DB: thelot
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
